# 🚀 部署指南

本指南提供多种部署方式，根据你的需求选择合适的方案。

---

## 📋 目录

1. [部署前准备](#部署前准备)
2. [方式1：本地运行（快速测试）](#方式1本地运行快速测试)
3. [方式2：云服务器部署（推荐）](#方式2云服务器部署推荐)
4. [方式3：Docker容器化部署（高级）](#方式3docker容器化部署高级)
5. [方式4：无服务器部署（现代）](#方式4无服务器部署现代)
6. [部署后验证](#部署后验证)
7. [域名和HTTPS配置](#域名和https配置)
8. [监控和维护](#监控和维护)

---

## 部署前准备

### 1. 检查配置文件

确保 `.env` 文件配置正确：

```bash
cat .env
```

必须包含：
```bash
WECHAT_QRCODE_URL=https://ibb.co/0y0jXhCv
WECHAT_GROUP_QRCODE_URL=https://ibb.co/PZrnNCT2
PAYMENT_PRICE=68.00
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

### 3. 运行测试

```bash
# 测试收款工具
python tests/test_simple_payment.py

# 测试群工具
python tests/test_wechat_group_info.py

# 测试完整流程
python tests/test_complete_flow.py
```

确保所有测试通过。

---

## 方式1：本地运行（快速测试）⭐

### 适用场景
- 个人测试
- 快速体验
- 本地开发调试

### 步骤

#### 1. 启动服务

```bash
# 方式1：使用默认端口（8080）
python src/main.py -m http

# 方式2：指定端口
python src/main.py -m http -p 8000
```

#### 2. 访问服务

打开浏览器或使用curl：

```bash
# 检查服务是否启动
curl http://localhost:8080/health

# 调用聊天接口
curl -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好，我想创业"}'
```

#### 3. 测试完整流程

```bash
# 使用脚本测试
python tests/demo_auto_welcome.py
```

### 优点
- ✅ 最简单快速
- ✅ 无需额外成本
- ✅ 适合开发调试

### 缺点
- ❌ 只能在本地访问
- ❌ 不方便分享给用户
- ❌ 电脑关机后服务停止

---

## 方式2：云服务器部署（推荐）⭐⭐⭐

### 适用场景
- 正式运营
- 给用户访问
- 需要稳定服务

### 推荐配置

| 资源 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 1核 | 2核 |
| 内存 | 2GB | 4GB |
| 带宽 | 1Mbps | 3Mbps |
| 存储 | 20GB | 40GB |
| 系统 | Ubuntu 20.04+ | Ubuntu 22.04 |

### 推荐云服务商

| 服务商 | 轻量应用服务器价格 | 特点 |
|--------|-----------------|------|
| **阿里云** | ¥60/月（2核2G） | 国内访问快 |
| **腾讯云** | ¥50/月（2核2G） | 性价比高 |
| **华为云** | ¥60/月（2核2G） | 企业级 |
| **AWS** | $5/月（t3.micro） | 国际访问快 |

### 详细步骤（以阿里云为例）

#### 1. 购买服务器

1. 访问：https://www.aliyun.com/product/swas
2. 选择配置：
   - 镜像：Ubuntu 22.04
   - 套餐：2核2G（60元/月）
   - 带宽：3Mbps
   - 存储：40GB
3. 设置密码，购买

#### 2. 连接服务器

```bash
# 使用SSH连接
ssh root@你的服务器公网IP

# 输入密码
```

#### 3. 安装Python环境

```bash
# 更新系统
apt update && apt upgrade -y

# 安装Python 3.10+
apt install python3.10 python3-pip python3-venv -y

# 验证版本
python3 --version
```

#### 4. 创建虚拟环境

```bash
# 创建项目目录
mkdir -p /opt/opc-agent
cd /opt/opc-agent

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级pip
pip install --upgrade pip
```

#### 5. 上传项目文件

**方法1：使用SCP（推荐）**
```bash
# 在本地电脑执行
scp -r /workspace/projects/* root@你的服务器IP:/opt/opc-agent/
```

**方法2：使用Git**
```bash
# 在服务器上执行
cd /opt/opc-agent
git clone 你的项目仓库地址
```

**方法3：使用SFTP工具**
- WinSCP（Windows）
- FileZilla（跨平台）
- 直接拖拽上传文件

#### 6. 安装依赖

```bash
cd /opt/opc-agent

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

#### 7. 配置环境变量

```bash
# 复制.env.example为.env
cp .env.example .env

# 编辑.env文件
nano .env
```

填写配置：
```bash
WECHAT_QRCODE_URL=https://ibb.co/0y0jXhCv
WECHAT_GROUP_QRCODE_URL=https://ibb.co/PZrnNCT2
PAYMENT_PRICE=68.00
PRODUCT_NAME=OPC创业指导PDF
```

保存并退出（Ctrl+X，Y，Enter）。

#### 8. 安装服务管理工具

```bash
# 安装supervisor（进程管理）
pip install supervisor

# 创建supervisor配置
mkdir -p /etc/supervisor/conf.d
```

创建配置文件 `/etc/supervisor/conf.d/opc-agent.conf`：

```bash
nano /etc/supervisor/conf.d/opc-agent.conf
```

内容：
```ini
[program:opc-agent]
command=/opt/opc-agent/venv/bin/python /opt/opc-agent/src/main.py -m http -p 8080
directory=/opt/opc-agent
user=root
autostart=true
autorestart=true
startretries=3
stderr_logfile=/var/log/opc-agent.err.log
stdout_logfile=/var/log/opc-agent.out.log
```

#### 9. 启动服务

```bash
# 启动supervisor
supervisord -c /etc/supervisor/supervisord.conf

# 更新配置
supervisorctl update

# 启动服务
supervisorctl start opc-agent

# 查看状态
supervisorctl status
```

#### 10. 配置防火墙

```bash
# 允许8080端口
ufw allow 8080/tcp

# 查看状态
ufw status
```

#### 11. 配置Nginx反向代理（可选，推荐）

```bash
# 安装Nginx
apt install nginx -y

# 创建配置
nano /etc/nginx/sites-available/opc-agent
```

内容：
```nginx
server {
    listen 80;
    server_name 你的域名或IP;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }
}
```

```bash
# 启用配置
ln -s /etc/nginx/sites-available/opc-agent /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启Nginx
systemctl restart nginx
```

#### 12. 访问服务

在浏览器中打开：
```
http://你的服务器公网IP
```

或使用curl测试：
```bash
curl http://你的服务器公网IP/api/health
```

### 优点
- ✅ 稳定可靠
- ✅ 24/7运行
- ✅ 可远程访问
- ✅ 容易扩展

### 缺点
- ❌ 需要购买服务器
- ❌ 需要一定的运维知识

---

## 方式3：Docker容器化部署（高级）⭐⭐⭐

### 适用场景
- 需要环境一致性
- 微服务架构
- 易于迁移和扩展

### 步骤

#### 1. 创建Dockerfile

在项目根目录创建 `Dockerfile`：

```dockerfile
# 使用官方Python镜像
FROM python:3.10-slim

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY requirements.txt .

# 安装依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制项目文件
COPY . .

# 暴露端口
EXPOSE 8080

# 启动命令
CMD ["python", "src/main.py", "-m", "http", "-p", "8080"]
```

#### 2. 创建docker-compose.yml

```yaml
version: '3.8'

services:
  opc-agent:
    build: .
    ports:
      - "8080:8080"
    environment:
      - COZE_WORKSPACE_PATH=/app
      - COZE_WORKLOAD_IDENTITY_API_KEY=${COZE_WORKLOAD_IDENTITY_API_KEY}
      - COZE_INTEGRATION_MODEL_BASE_URL=${COZE_INTEGRATION_MODEL_BASE_URL}
    env_file:
      - .env
    restart: always
    volumes:
      - ./logs:/app/logs
```

#### 3. 构建和运行

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

#### 4. 访问服务

```
http://localhost:8080
```

### 优点
- ✅ 环境一致
- ✅ 易于迁移
- ✅ 便于扩展
- ✅ 自动重启

### 缺点
- ❌ 需要Docker知识
- ❌ 占用资源稍大

---

## 方式4：无服务器部署（现代）⭐⭐⭐

### 适用场景
- 低流量应用
- 按需付费
- 无需管理服务器

### 推荐平台

| 平台 | 价格 | 特点 |
|------|------|------|
| **阿里云函数计算** | 100万次调用免费 | 国内访问快 |
| **腾讯云SCF** | 100万次调用免费 | 与微信生态集成 |
| **AWS Lambda** | 100万次调用免费 | 国际化 |
| **Vercel** | 免费额度 | 部署简单 |

### 简单步骤（阿里云函数计算为例）

#### 1. 安装阿里云CLI

```bash
pip install aliyun-cli
```

#### 2. 配置账号

```bash
aliyun configure
```

#### 3. 部署函数

使用Fun工具或直接在控制台部署。

**注意**：无服务器部署需要对代码进行改造（适配函数计算模型）。

### 优点
- ✅ 无需管理服务器
- ✅ 按需付费
- ✅ 自动扩展
- ✅ 免费额度

### 缺点
- ❌ 冷启动慢
- ❌ 有执行时间限制
- ❌ 需要改造代码

---

## 部署后验证

### 1. 健康检查

```bash
# 检查服务是否正常运行
curl http://你的域名或IP/api/health
```

预期响应：
```json
{
  "status": "ok"
}
```

### 2. 功能测试

```bash
# 测试聊天功能
curl -X POST http://你的域名或IP/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好，我想创业"}'
```

### 3. 端到端测试

```bash
# 使用Python脚本测试
python tests/test_complete_flow.py
```

---

## 域名和HTTPS配置（推荐）

### 步骤

#### 1. 购买域名

推荐域名注册商：
- 阿里云万网
- 腾讯云
- Namecheap

#### 2. 配置DNS解析

在域名管理后台添加A记录：
```
类型：A
主机记录：@
记录值：你的服务器公网IP
TTL：600
```

#### 3. 安装SSL证书

**使用Let's Encrypt（免费）**：

```bash
# 安装certbot
apt install certbot python3-certbot-nginx -y

# 获取证书
certbot --nginx -d 你的域名

# 自动续期
certbot renew --dry-run
```

#### 4. 更新Nginx配置

```nginx
server {
    listen 443 ssl http2;
    server_name 你的域名;

    ssl_certificate /etc/letsencrypt/live/你的域名/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/你的域名/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name 你的域名;
    return 301 https://$server_name$request_uri;
}
```

#### 5. 重启Nginx

```bash
nginx -t
systemctl restart nginx
```

现在可以通过HTTPS访问：
```
https://你的域名
```

---

## 监控和维护

### 1. 日志监控

```bash
# 查看应用日志
tail -f /var/log/opc-agent.out.log

# 查看错误日志
tail -f /var/log/opc-agent.err.log

# 查看Nginx日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### 2. 服务状态监控

```bash
# 查看supervisor状态
supervisorctl status

# 重启服务
supervisorctl restart opc-agent

# 停止服务
supervisorctl stop opc-agent
```

### 3. 系统资源监控

```bash
# CPU和内存使用
top

# 磁盘使用
df -h

# 网络连接
netstat -tulpn
```

### 4. 定期备份

```bash
# 创建备份脚本
nano /opt/backup.sh
```

内容：
```bash
#!/bin/bash
DATE=$(date +%Y%m%d)
tar -czf /backup/opc-agent-$DATE.tar.gz /opt/opc-agent
find /backup -name "opc-agent-*.tar.gz" -mtime +7 -delete
```

```bash
# 添加到crontab（每天凌晨3点备份）
chmod +x /opt/backup.sh
crontab -e
```

添加：
```
0 3 * * * /opt/backup.sh
```

### 5. 定期更新

```bash
# 更新系统
apt update && apt upgrade -y

# 更新Python依赖
cd /opt/opc-agent
source venv/bin/activate
pip install --upgrade -r requirements.txt

# 重启服务
supervisorctl restart opc-agent
```

---

## 推荐部署方案

### 对于你当前的需求

**推荐：方式2 - 云服务器部署**

理由：
1. ✅ 简单直接，无需复杂配置
2. ✅ 成本低（每月60元左右）
3. ✅ 稳定可靠，适合正式运营
4. ✅ 便于维护和扩展

### 快速部署清单

1. 购买阿里云轻量应用服务器（2核2G，60元/月）
2. 使用SCP上传项目文件
3. 安装依赖并配置环境变量
4. 使用supervisor管理服务
5. 配置Nginx反向代理（可选）
6. 配置域名和HTTPS（可选）

预计时间：30分钟

---

## 📞 需要帮助？

如果在部署过程中遇到问题：
1. 查看本文档对应章节
2. 检查日志文件
3. 联系云服务商客服

**祝你部署成功！** 🚀
